export interface Farm { id: number; name: string; location?: string; }
export interface CreateFarm { name: string; location?: string; }
export interface UpdateFarm { name: string; location?: string; }
